{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "npmClient": "pnpm",
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["main", "next", "v*"]
    },
    "publish": {
      "message": "chore: publish new version"
    }
  }
}
